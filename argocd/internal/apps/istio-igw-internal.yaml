apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-igw-internal
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: 'git@github.com:willyhutw/microlab.git'
      path: argocd/internal/istio-igw-internal
      targetRevision: HEAD
      helm:
        valueFiles:
          - values.yaml
        releaseName: istio-igw-internal
  destination:
    name: micro
    namespace: istio-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
