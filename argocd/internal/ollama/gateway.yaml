apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: ollama-gateway
  namespace: ai
spec:
  selector:
    istio: ingressgateway
    igw-type: internal
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - ollama.willyhu.tw
      tls:
        httpsRedirect: true
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - ollama.willyhu.tw
      tls:
        credentialName: ollama-willyhu-tw-tls
        mode: SIMPLE
