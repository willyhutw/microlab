apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: langfuse-willyhu-tw-tls
  namespace: istio-system
spec:
  secretName: langfuse-willyhu-tw-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - langfuse.willyhu.tw
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  usages:
    - digital signature
    - key encipherment
