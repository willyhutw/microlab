apiVersion: v1
kind: ConfigMap
metadata:
  name: gf-cm-default-dashboards
  namespace: {{ .Values.namespace }}
data:
{{- $files := .Files.Glob .Values.path.default }}
{{- range $path, $file := $files }}
  {{ base $path }}: |-
{{ toString $file | indent 4 }}
{{- end }}