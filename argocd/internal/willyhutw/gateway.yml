apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: willyhu-tw-gateway
  namespace: internal
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - www-dev.willyhu.tw
      tls:
        httpsRedirect: true
    - port:
        number: 443
        name: https
        protocol: HTTPS
      hosts:
        - www-dev.willyhu.tw
      tls:
        credentialName: www-dev-willyhu-tw-tls
        mode: SIMPLE
