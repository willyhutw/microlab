apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: qdrant-willyhu-tw-tls
  namespace: istio-system
spec:
  secretName: qdrant-willyhu-tw-tls
  duration: 2160h
  renewBefore: 360h
  dnsNames:
    - qdrant.willyhu.tw
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  usages:
    - digital signature
    - key encipherment
